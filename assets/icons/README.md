# My Torrent icons

`app_icon.png` is exported from the Pencil `Application Icon` design node and is used by the MSIX package.

Generate `app_icon.ico` and `windows/runner/resources/app_icon.ico` from that PNG so the Windows tray and executable use the same artwork.
